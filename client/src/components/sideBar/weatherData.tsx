import React from "react";

const WeatherData: React.FC = props => {
    return (
        <div id="weather-data" className="menu-wrapper">
            "WEATHER DATA"
        </div>
    );
}

export default WeatherData;